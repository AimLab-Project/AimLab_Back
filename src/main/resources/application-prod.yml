spring:
  # Production DB
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/aimlab?allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: qwe123

  jpa:
    hibernate:
      ddl-auto: validate

# 실제 운영중인 서버의 Port 번호는 8080
server:
  port: 8080